# TODO

- [ ] add build and recompile db script

## Rebuild and recompile db

Execute when the platformio.ini changes/updates and/or editor can't find the new depenedencies/libraries

COMMAND: `pio run && pio run -t recompiledb`
